# Claude Code 진입 지침

`AGENTS.md`의 현재 요청별 읽기 경로를 따른다. 전체 운영 문서나 다른 역할을 시작 시 일괄 읽지 않는다. 두 모드의 새 직접 제작은 현재 세션의 모델·effort를 그대로 사용한다. 관리형 실행에만 `manage_run.py next --brief`의 런타임 모델·effort를 적용하며, 생성된 `.claude/agents/` 선언을 임의 변경하지 않는다.
